import { fetchMatches } from './matches.js';
import { fetchGlobalUsers } from './globalProfiles.js';
// This importing and exporting is for when things start to scale up

export {
  fetchMatches,
  fetchGlobalUsers
}
